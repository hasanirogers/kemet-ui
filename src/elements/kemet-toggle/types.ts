export interface KemetToggleInterface extends HTMLElement {
  name?: string,
  checked?: boolean,
  disabled?: boolean,
  label?: string,
  show?: boolean,
  squared?: boolean,
  optionChecked?: string,
  optionUnchecked?: string,
}
