import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as stories from '../elements/field.stories';
import '../../../.storybook/elements/docs-api';

<Meta of={stories} />

# Kemet Field

Used in combination with Input, Select, and Textarea, to make a Field.

<kemet-tabs panel-effect="stacked" divider>
  <kemet-tab slot="tab">Usage</kemet-tab>
  <kemet-tab slot="tab">Story</kemet-tab>
  <kemet-tab-panel slot="panel">
    ### Markup
    ```html
    <kemet-field label="The Label">
      <kemet-input slot="input" name="input"></kemet-input>
    </kemet-field>
    ```

    ### Import JavaScript
    ```javascript
    import 'kemet-ui/elements/field'
    ```
  </kemet-tab-panel>
  <kemet-tab-panel slot="panel">
    <Canvas of={stories.Input}  />
    <Controls of={stories.Input} />
  </kemet-tab-panel>
</kemet-tabs>

<docs-api tag="kemet-field" />

