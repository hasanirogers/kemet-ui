import { Meta, Canvas, ArgTypes, Controls } from '@storybook/addon-docs/blocks';
import * as stories from '../elements/combo.stories';
import '../../../.storybook/elements/docs-api';

<Meta of={stories} />

# Kemet Combo

Allows the user to select a choice filtered through an Input. May only be used as a component of Field.

<kemet-tabs panel-effect="stacked" divider>
  <kemet-tab slot="tab">Usage</kemet-tab>
  <kemet-tab slot="tab">Story</kemet-tab>
  <kemet-tab-panel slot="panel">
    ### Markup
    ```html
    <kemet-field>
      <kemet-input slot="input" name="input"></kemet-input>
      <kemet-combo slot="component" options='["Item 1", "Item 2", "Item 3"]'></kemet-combo>
    </kemet-field>
    ```

    ### Import JavaScript
    ```javascript
    import 'kemet-ui/elements/combo'
    ```
  </kemet-tab-panel>
  <kemet-tab-panel slot="panel">
    <Canvas of={stories.Standard}  />
    <Controls of={stories.Standard} />
  </kemet-tab-panel>
</kemet-tabs>

<docs-api tag="kemet-combo" />
